import Ember from 'ember';
// import RSVP from 'rsvp';

export default Ember.Route.extend({
  // model ({photo_id}) {
  //   return RSVP.hash({
  //     photo: this.get('store').find('photo', photo_id),
  //     tags: this.get('store').findAll('tag'),
  //   });
  // }
});
